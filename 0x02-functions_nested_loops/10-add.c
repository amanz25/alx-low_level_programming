#include "main.h"

/**
 * add - program that adds two integers and returns the result
 * @num1: number to be added
 * @num2: number to be added
 * Return: Always sum of two numbers (num1 and num2) (Success)
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
